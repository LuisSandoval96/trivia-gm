services:
  - type: web
    name: trivia-gm
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server-simple.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        value: admin123GM
